Optimisers
==========

The `optimiser` defines how to minimize the objective functions and  sampling of the model space.
In `grond` so far only a single `optimiser` is enabled. The setup can be used as example 
to derive any kind of optimisation or inversion scheme, however.


